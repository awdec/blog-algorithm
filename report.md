# ACM/ICPC 竞赛算法项目审查报告

> 审查日期：2026-08-11  
> 审查对象：当前工作区，而不是某个历史提交  
> 审查口径：算法竞赛训练、赛时复用与模板可靠性；不套用大学算法课程的覆盖度要求，也不按工业项目的架构、封装、异常处理和测试体系评分  
> 报告性质：从当前文件重新建立基线；旧版 `report.md` 的问题数量与结论不再沿用

## 一、结论

本轮重构已经把站点结构修到**可构建、可部署、当前导航无死链**的状态，也确实关闭了旧报告中的大量算法错误；但正文仍不能整体标记为“可直接复制上场的稳定模板库”。

按本报告的竞赛口径，当前共保留 **57 组**可复现问题：

| 级别 | 数量 | 本报告中的含义 |
| --- | ---: | --- |
| P0 | 8 | 在正文明确范围或普通合法竞赛输入内，可直接造成 WA、UB、越界或错误结果；应在再次宣称“全部修复”前闭环 |
| P1 | 22 | 依赖常见但未写出的约束、初始化/多测协议，或缺少算法关键步骤，实战中很容易误用 |
| P2 | 19 | 复杂度、接口语义、适用条件或参考代码身份不够准确；不会在所有调用中立刻错，但会影响选型和迁移 |
| P3 | 8 | 注释、符号、导航与站点完成度问题，不直接决定 AC |

这里按“问题组”计数：同一局部根因引出的相邻症状合并为一组。空白章节、明确占位页、没有覆盖某个知识点等，不按课程完整性判错。

当前最需要先处理的 8 个 P0 是：

1. 64 位线性基第 `k` 小仍用 32 位 `int`；
2. Floyd 最小环模板对 `INF + INF` 做有符号溢出；
3. 多维整除分块会除以 0；
4. 固定范围在线逆元代码仍在 `u64` 转换前做 `int * int`；
5. 线性同余方程在取模前把 exgcd 系数乘爆 `int`；
6. 计算几何全家桶把 `eps` 放进排序比较器；
7. Andrew 凸包对单点返回空集；
8. 快读无法安全读取合法的 `INT_MIN`。

因此，本项目当前适合作为**高质量个人竞赛笔记与参考代码集合**，但尚不适合把所有 `cpp` 代码块统一视为已经编译、对拍并可直接提交的模板。

## 二、审查范围与验证

### 2.1 正文覆盖

逐页静态复核了 `docs` 下 68 篇算法正文：

| 模块 | Markdown | 行数 | C++ 代码块 |
| --- | ---: | ---: | ---: |
| 数据结构 | 22 | 6422 | 79 |
| 动态规划 | 1 | 381 | 16 |
| 计算几何 | 11 | 2303 | 6 |
| 图论 | 7 | 1534 | 28 |
| 数学 | 9 | 1980 | 24 |
| 杂项 | 6 | 863 | 11 |
| 多项式 | 2 | 513 | 14 |
| 字符串 | 10 | 983 | 24 |
| **合计** | **68** | **14979** | **202** |

另外检查了：

- `docs/index.md`、`docs/markdown-examples.md`；
- 根目录 `数据结构整理.md`、`字符串算法整理.md`；
- VitePress 配置与主题、README、部署工作流、旧 URL 重定向脚本；
- `PDF_BOOK_EXPORT_PLAN.md` 中与当前目录结构有关的规划内容。

`report.md` 是本轮输出，不作为输入计入；`report.pdf` 未在本轮重新生成。

### 2.2 分级标准

- **P0**：正文已经声明支持，或没有排除的普通竞赛输入，可稳定触发错误答案、越界、未定义行为或错误数据结构结果。
- **P1**：需要补关键算法步骤，或必须依赖常见但未写明的限制，例如只允许全局零初始化、只允许单测、只接收有向孩子表、只处理非负数等。
- **P2**：复杂度、语义、边界协议或“完整模板/伪代码”身份不准确，容易影响选型或二次改写。
- **P3**：文字、符号、注释、导航、占位可识别性等低风险问题。

以下事项**不判错**：

- `block.md`、`tree-block.md`、`lca.md`、`Generating-function.md` 等明确占位页内容较少；
- 带 `/* ... */` 的明显框架代码不能独立编译；但若框架给出的算法顺序本身会诱导 WA，仍会按 P1 记录；
- 没有工业级参数检查、异常类型、命名规范、模块封装或 CI 单元测试；
- 没有覆盖一门课程意义上的全部算法知识。

### 2.3 自动验证结果

| 检查项 | 结果 |
| --- | --- |
| `npm.cmd run docs:build` | 通过；生成 33 个旧 URL 重定向页，仅有大 chunk 警告 |
| `docs` 下 Markdown | 70 个；全部为有效 UTF-8 |
| C++/C++ 标记代码块 | 202 个；代码围栏全部配对 |
| VitePress 配置中的本地目标 | 69 个唯一非空路由，缺失目标 0 个 |
| 配置空链接 | 1 个，即多项式侧栏空项 |
| 正文绝对 HTML 静态资源 | 12 个唯一引用，缺失文件 0 个 |
| 合并冲突标记 | 0 处 |
| C++ 编译验证 | 未执行；当前环境没有 `g++`、`clang++` 或 MSVC `cl` |

VitePress 构建通过只证明 Markdown、站点配置和前端构建能够工作，**不能证明 202 个 C++ 代码块可编译或算法正确**。本报告中的反例来自静态执行、边界推演和局部穷举；其中 Manacher 又额外对二进制串长度 0–8 与暴力结果做了穷举核对。

## 三、P0：必须先修复的竞赛正确性问题

### P0-01 `docs/data-structure/xor-base.md:150-174`

`query_kth(int k)` 的编号和累加器仍是 32 位：`int res` 会截断 64 位基向量；循环到 `i >= 32` 时执行 `k >> i` 又是超位宽移位 UB。正文第 137 行已经要求使用足够宽的无符号类型，代码与正文正面冲突。

- 最小反例：规范基只有 `1ULL << 40`，查询相应非零值应得到 `2^40`，当前 `res` 在常见实现上被截成 0。
- 修复方向：编号与结果使用足够宽的无符号类型，只遍历实际秩；校验 `k` 的合法范围。秩为 64 时还要单独设计如何表示答案总数 `2^64`。

### P0-02 `docs/graph/shortest-path.md:249-282`

Floyd 最小环代码在第 264、276 行没有检查可达性。沿用本页 `INF = 1LL << 62` 时，`INF + INF == 2^63`，超出 `long long`，发生有符号溢出 UB并可能把距离污染成负数。

- 最小反例：3 个点、0 条边；更新阶段会计算 `INF + INF`。
- 额外前提：该“最大编号点 `k` + 两条原边”的公式是无向图最小简单环模板；有向环 `1 -> 3 -> 2 -> 1` 会被固定方向枚举漏掉。
- 修复方向：三段均有限时才计算候选环，Floyd 松弛也先检查两段有限；正文明确无向图、边权和环定义。

### P0-03 `docs/math/number-theory/sieve.md:34-38`

多维整除分块对每个 `a[k]` 无条件计算 `a[k] / (a[k] / i)`。一旦 `i > a[k]`，内层商为 0，随后直接整数除零。

- 最小反例：`m = 2, a = {1, 2}`。`maxn = 2`，当 `i = 2, k = 1` 时计算 `1 / 0`。
- 修复方向：商为 0 的维度在当前上界内不再变化，不应用它限制右端点；同时统一正文求和上界与代码的 `max(a)`，说明 `i=1` 是否另算。

### P0-04 `docs/math/number-theory/mod1.md:114-133`

正文第 69 行已要求乘法前转为 `u64`，代码第 115、120、132、133 行却仍先做 `int * int`，溢出后才把结果传给 Barrett。

- 反例：合法范围内的质数 `p = 1,000,003`。逆元递推到 `i = 4` 时，`iv[3] * (p - p/4) = 666669 * 750003`，远超 `INT_MAX`。
- 修复方向：每个乘法的第一个操作数显式转成 `u64`/`u128`，并用同一宽类型贯穿 `res2`、`iv` 和最终恢复式。

### P0-05 `docs/math/number-theory/mod.md:92-101`

线性同余方程模板把 exgcd 得到的 `int x` 原地乘 `b/g`，在最终取模前就可能溢出。

- 反例：`a=701408733, b=1134903169, c=1134903170`，`g=1`；exgcd 系数与 `b` 的数学乘积约为 `4.9e17`，而正确最小非负解仍可放入 `int`。
- 修复方向：系数、乘法和规范化过程至少使用 `long long`，稳妥做法是用 `__int128` 乘后再模 `c/g`。

### P0-06 `docs/geometry/model.md:23-31`

`point::operator<` 把 `eps` 写进排序关系；同类问题还出现在极角、最近点对和事件比较器中。这样的比较不满足严格弱序，交给 `sort/set/multiset` 后不再有可靠语义。

- 反例：令三点横坐标为 `0`、`0.75*eps`、`1.5*eps`，纵坐标相同。前两点“等价”、后两点“等价”，首尾却有严格次序，等价关系不传递。
- 修复方向：容器/排序键使用精确字典序、离散后的整数键或稳定唯一编号；`eps` 只用于几何谓词，不用于 STL 排序关系。

### P0-07 `docs/geometry/model.md:589-612`

Andrew 凸包对单点输入返回空：第一趟压入唯一点，随后 `p.pop_back()` 清空输入，第二趟不执行，最后又把 `st` 的唯一点弹掉。

- 最小反例：点集 `{(0,0)}`，正确凸包应含该点，当前返回空。
- 修复方向：先去重；去重后 `n <= 2` 直接返回对应点集，再进入通用 Andrew 流程。

### P0-08 `docs/other/constant.md:111-132`

快读正文声称支持 `int` 范围，但读取 `-2147483648` 时，会先在 signed `int` 中构造正幅值 `2147483648`，第 128 行已经溢出；随后乘 `-1` 仍不安全。

- 最小反例：输入单个 `-2147483648`。
- 修复方向：用 `unsigned int` 或更宽整数累积绝对值，按正负边界检查后安全落到 `int`。

## 四、P1：高风险模板与关键前提

### 4.1 数据结构与 DP

- **P1 DS-01 — `docs/data-structure/dsu-on-tree.md:107-130`**：骨架只在 `keep=true` 时“添加 `x` 的信息”。标准 sack 顺序应当无条件 `add(x)`、回答 `x`，最后才按 `keep` 决定是否清空。两点树中轻叶以 `keep=false` 处理时，按当前骨架会得到子树颜色数 0 而不是 1。它是明确框架，因此不按编译失败判 P0，但流程本身会诱导 WA。
- **P1 DS-02 — `docs/data-structure/bitset.md:411-416`**：`count(const FastBitset&)` 不像相邻按位接口那样检查等长，而是按当前对象的 `m` 读取对方数组。`FastBitset a(65), b(1); a.count(b)` 会越界读 `b.a[1]`。单点、切片和查找接口也应统一写明合法下标契约。
- **P1 DS-03 — `docs/data-structure/dsu.md:259-275`**：`merge_dsu::init(value_limit)` 不清 `belong/exist`。第一测把 `1 -> 2` 后，第二测仅重新 `init`，再做本应无效的 `2 -> 3`，会继承旧映射并错误改变初始值 1。若只允许单次初始化，应写入契约；否则必须清状态。
- **P1 DS-04 — `docs/data-structure/tree-chain.md:106-145`**：长链祖先表硬编码 `i < 19`，却未限制 `n/k`。链长 524289、从最深点询问第 524288 级祖先时会访问未预处理的 `f[x][19]`；`k` 超过深度时也没有无解协议。应由最大 `n` 计算 `LOG` 并先判祖先是否存在。
- **P1 DS-05 — `docs/data-structure/tree-in-tree.md:134,196-242`**：第 134 行称“区间第 `k` 大”，代码统计左子树并优先向左，实际返回第 `k` 小，第 242 行又改回“第 `k` 小”。区间 `{1,2}`、`k=1` 时代码返回 1。还应验证 `1 <= k <= 区间元素数`，否则会返回不存在的值域端点。
- **P1 DS-06 — `docs/data-structure/wavelet.md:164-223`**：小波矩阵只对非负 `int` 正确，正文未给这一契约。输入 `{-1,0}` 时，`__lg(-1)`、算术右移和 `1 << 31` 均有风险，二进制无符号序也不是有符号大小序。应声明非负，或先以无符号数翻转符号位；同时校验区间和 `k`。
- **P1 DS-07 — `docs/data-structure/mo.md:328-338`**：定义 `h(x)=g(a[x],x-1)` 后，把 `sum_{x=r+1}^{r'} h(x)` 写成 `h(r')-h(r)`，少了一层前缀和。取 `h(x)=x,r=1,r'=3`，真实贡献是 5，文式得到 2。应定义 `H(t)=sum_{x<=t}h(x)`，使用 `H(r')-H(r)`。
- **P1 DS-08 — `docs/data-structure/kdtree.md:33-35,78-104,260-291`**：节点权、子树和与查询结果均为 `int`，正文没有总和范围。查询框内 3 个权值 `1e9` 的点，正确和为 `3e9`，当前会溢出；坐标跨度 `mx-mn` 也应使用宽类型或声明范围。

### 4.2 图论与字符串

- **P1 GS-01 — `docs/graph/circle.md:176-182`**：有向三元环应交“`v` 的直接入邻点”和“`w` 的直接出邻点”，不能使用传递意义的可达集；对每条边累加后，同一三元环还会计 3 次，必须除以 3。仅有 `1->2->3->1` 时原始交集和为 3，实际答案为 1。复杂度宜写 `O(mn/word)`，稠密时才是 `O(n^3/word)`。
- **P1 GS-02 — `docs/string/pam.md:133-178`**：Palindrome Series 示例的 `diff/slink` 赋值被注释，DP 又使用未构造的 `a[i]`；同时正文对所有 `i` 定义递推，代码只在 `i` 为偶数时累加。`s="a"` 时正文应得 `f[1]=1`，代码得到 0。需要先完整定义状态，再重新核对奇偶过滤位置。
- **P1 GS-03 — `docs/string/sa.md:35-80`**：`SA sa;` 的局部对象会先读取未初始化的 `n/sa[0]/oldrk`；即使依赖全局零初始化，跨测也不清 `oldrk[n+1..]` 哨兵。同一对象先处理 `"aaaa"` 再处理 `"aaa"`，旧 `oldrk[4]` 会阻止排名正确分裂，随后 `w > n` 并越界。应提供真正的初始化并清尾部哨兵。
- **P1 GS-04 — `docs/string/acam.md:23,66-75`**：扫描只做 `vis[now]++`，不是各模式的最终出现次数。模式 `{a,ba}`、文本 `"ba"` 时，`a` 节点仍为 0。应保存 BFS 序并逆序执行 `vis[fail[v]] += vis[v]`，或明确 `vis` 只是状态命中次数并另给输出链处理。
- **P1 GS-05 — `docs/string/sam.md:57-88`**：除 `idx` 外数组没有初始化，局部 `SAM sam;` 首次扩展即读取不确定的根转移；容量还需接近 `2|S|`，字符下标只允许 `'a'..'z'`。字符串 `"abb"` 已可能创建 5 个状态，不能按 `|S|+1` 开槽。应补初始化、容量与字符域契约。
- **P1 GS-06 — `docs/string/trie.md:13-35`**：`query` 返回非零节点只证明前缀路径存在，不证明字符串已插入。只插入 `"abc"` 后查询 `"ab"` 会返回非零；精确成员查询还要检查 `cnt[p]`。同段也应声明仅支持小写字母。
- **P1 GS-07 — `docs/string/kmp.md:13-24`**：模板要求文本和模式都在下标 0 放哨兵、模式非空，并预先为模式计算 `border`，但本段未写。直接传普通 `a="ba", b="a"` 会把模式长度算成 0 并访问 `b[1]`。应在接口旁写明 1-based 契约或改成普通 0-based 模板。
- **P1 GS-08 — `docs/graph/tarjan.md:131-142`**：`(id ^ parent_id) == 1` 只有在无向边两方向编号恰好互为 `eid ^ 1` 时成立，本节未声明。单边 `1-2` 若方向编号为 2 和 1，子节点不会跳过反边，桥两端会被错误并入同一边双。

### 4.3 数学、几何与杂项

- **P1 MG-01 — `docs/math/number-theory/sieve.md:89-113`**：杜教筛代码调用未定义的 `sum1/sum2`；`sum_euler`、局部 `res` 与记忆表又都用 `int`。已知 `sum_{i<=100000} phi(i)=3039650754 > INT_MAX`，且 `(1+n)*n` 会先在 `int` 中溢出。需要统一函数名，并让返回链与中间乘法使用 `long long/__int128`。
- **P1 MG-02 — `docs/math/number-theory/mod.md:332-350`**：BSGS 在 `b %= p` 前判断 `b == 1`，对未规范化输入不能返回最小解。`a=2,b=8,p=7` 中 `b≡1`，最小解是 0，当前返回 3。应先规范化 `a,b`，再处理 0/1 特判。
- **P1 MG-03 — `docs/geometry/model.md:703-725`**：默认 `point_t=long double`，最近点对仍用 `sqrt(ans)+1` 的整数窗口并宣称 `O(n log n)`。点 `p_i=(i/n,0)` 的最小距约为 `1/n`，窗口却约为 1，活动集合和枚举可退化到 `Theta(n^2)`。浮点版应使用实际距离配合尺度化容差；只有整数坐标才能解释固定 `+1`。
- **P1 MG-04 — `docs/geometry/model.md:1093-1116`**：最小圆覆盖空输入直接访问 `a[0]`。随机增量的复杂度还应写“随机打乱后的期望 `O(n)`”，而不是无条件 `O(n)`；空集需定义返回协议。
- **P1 MG-05 — `docs/geometry/model.md:1123-1144`**：自适应 Simpson 只有强制最少递归层数，没有最大深度或 `mid==l/r` 停止条件。误差持续不满足或出现 NaN 时会递归到栈溢出；`O(log eps T f(x))` 也不是成立的通用复杂度。应以实际递归节点数 `K` 计 `O(K*T_f)` 并设置停止协议。

### 4.4 根目录汇总文档

- **P1 SUM-01 — `字符串算法整理.md:20-22`**：第 20 行允许 Border 等于原串，第 22 行却无条件用“最大 Border”求最小周期。`"aaaa"` 若取自身 Border 会得到周期 0；应明确使用最大真 Border，正确最小周期为 1。

## 五、P2：复杂度、契约与模板身份

### 5.1 数据结构与 DP

- **P2 DS-09 — `docs/dp/bag.md:323-336`**：树上背包若 `p[x]` 是已经定根的孩子表则正确；若是常见无向邻接表，两点树会 `1 -> 2 -> 1` 无限递归。应写明存图契约，或传 `parent` 并跳过父边。
- **P2 DS-10 — `docs/data-structure/sqrt-tree.md:386-415,438-479`**：vEB 声称单次 `O(log log V)`，但首次建簇时 `new VEB*[1<<high_bits]()` 会初始化 `Theta(sqrt V)` 个指针。应把这部分计入构建/摊还，或明确固定值域实现的前置预分配。
- **P2 DS-11 — `docs/data-structure/odt.md:100-102`**：`CF896C` 的 `O(m log log n)` 依赖该题的随机生成和推平模型，不能泛化为“数据随机”时的一般 ODT 保证；对抗操作仍可积累 `Theta(n)` 段。
- **P2 DS-12 — `docs/data-structure/binary-search-tree.md:111-127,285-304,447-469,610-624`**：同名 `get_rank_by_key` 的基准不统一：Treap/替罪羊返回严格更小元素数，FHQ/Splay 返回 1-based rank，而 `get_key_by_rank` 又统一按 1-based。集合 `{5}` 上前者返回 0、后者返回 1。应统一或把前者改名 `count_less`。
- **P2 DS-13 — `docs/data-structure/bit.md:9-16,25-29`**：两个 `init` 都用 `+=` 且不清 `tr`，只在对象全零且初始化一次时正确。`n=2,a=[1,0]` 连续初始化两次后 `tr[2]` 会由 1 变成 3。应写单次构建契约或主动清零。
- **P2 DS-14 — `docs/dp/bag.md:218-234`**：方案数的 `exp(sum ln F)` 和 `O(V log V)` 需要可做 FPS `ln/exp` 的域、`1..V` 可逆、快速卷积与截断条件；精确整数或模数特征不合适时不能直接套。
- **P2 DS-15 — `docs/data-structure/dsu.md:3`**：`O(n alpha(n))` 没有定义操作数。若有 `n` 个点、`q` 次操作，应写初始化加 `O(q alpha(n))`，或总 `O((n+q)alpha(n))`。

### 5.2 图论与字符串

- **P2 GS-09 — `docs/graph/shortest-path.md:363`**：Johnson 分配 `n*n` 答案并为每个源点初始化长度 `n` 的数组，复杂度不能只写 `O(nm log m)`；至少要包含 `O(n^2)` 和 Bellman-Ford/势能预处理成本。`m=0` 时正文表达式为 0，代码仍有 `Theta(n^2)` 工作。
- **P2 GS-10 — `docs/graph/shortest-path.md:422-426`**：把乘法不等式取对数时必须有 `x_a>0,x_b>0,c>0`，还要说明浮点临界误差；负数输入下原不等式有意义，`log(x_a)` 却无定义。
- **P2 GS-11 — `docs/graph/bipartite.md:105-107`**：二分图匹配转单位容量网络后还包含 `O(n)` 条源汇边和顶点初始化，不能只用原图边数 `m` 写 `O(m sqrt n)`。应改用流网络边数，或保留 `n+m` 项。
- **P2 GS-12 — `docs/graph/tarjan.md:169,245`**：边双缩点和点双圆方结构只有在原图连通时才是一棵树；一般图得到森林。两个互不相交的三角形就是两棵缩点树。

### 5.3 数学与计算几何

- **P2 MG-06 — `docs/math/number-theory/sieve.md:13-16`**：通用 `sum_{i=1}^n` 分块模板从 `i=2` 起，却未说明 `i=1` 另算；`n=1` 时漏掉唯一项。
- **P2 MG-07 — `docs/math/number-theory/sieve.md:70,74,78`**：杜教筛等式左侧把求和变量写成固定 `(f*g)(n)`，应为 `(f*g)(i)`；`sum_{d|n}^n` 也不是合法范围。代码即使修好，正文目前仍不能按式推导。
- **P2 MG-08 — `docs/math/number-theory/sieve.md:54-56`**：二次整除分块的 `i*i` 在 `int` 中先算。若按常见 `long long n` 使用，例如 `n=1e12`、`i=50000`，这里已经溢出；应使用 `1LL*i*i` 或明确只支持的范围。
- **P2 MG-09 — `docs/geometry/model.md:156-161`**：零长度线段的点距会构造零方向直线并做 `0/0`。线段 `(0,0)-(0,0)` 查询点 `(1,0)` 应退化为点距。
- **P2 MG-10 — `docs/geometry/model.md:346-351`**：主“全家桶”代码仍含 `[&](...)`，整个代码块不能按原样编译。它明显是占位，因此不按 P0；但应在代码块前明确标记为参考片段/伪代码。
- **P2 MG-11 — `docs/geometry/model.md:618-632`**：`halfinter` 无条件加入 `[-lim,lim]^2`，返回的是裁剪后的半平面交，不是一般交集。单约束 `x>=2e9` 本来非空，默认 `lim=1e9` 会返回空。应声明答案已知在框内，或单独处理无界交集。
- **P2 MG-12 — `docs/geometry/model.md:975-990`**：三维凸包对每个点做随机扰动，会改变原几何对象。四个共面方形点会变成薄四面体；只有题目允许 symbolic/general-position perturbation 时才能这样做，需写清输出语义。

### 5.4 根目录汇总文档

- **P2 SUM-02 — `数据结构整理.md:217`**：汇总仍写 `O(n log^{N-1} n T(n))`，而详细 `cdq.md` 已按“每层总扫描 + 单次数据结构操作”重新区分。`T(n)` 未定义时容易重复计算对数因子，应与详细页统一变量和维度解释。

## 六、P3：低风险文字与站点状态

- **P3-01 — `docs/data-structure/cdq.md:28-30`**：循环实际撤销 `[l,now)`，注释写成“删除 `[l,now]`”，差一位；代码当前正确。
- **P3-02 — `docs/data-structure/mo.md:308-314`**：树上莫队区间排版为 `[tin[x]],tin[y]]`；“不能处理树上回滚莫队”也应收窄为标准双出现 Euler 奇偶表示不能直接套单向回滚。
- **P3-03 — `docs/graph/shortest-path.md:247`**：`n=1000` 的 Floyd 能否通过取决于时限、实现和平台，不能作为通用规模结论；`10^9` 级内层更新通常必须实测。
- **P3-04 — `docs/math/combinatorics/sequence.md:214-217`**：`p(50)=204226>2*10^5`，第一条数值界应改为准确值或放宽上界。
- **P3-05 — `docs/math/number-theory/gcd.md:74-83`**：根号分治引理把“`c` 是质数”误写成 `v in P`，并把阈值写成 `sqrt(n)`；本节应使用值域 `v` 和大因子 `c`。
- **P3-06 — `数据结构整理.md:3`**：仍引用已迁移的旧目录 `docs/data structure`，应改为 `docs/data-structure`。
- **P3-07 — `docs/index.md:5-25`**：首页仍是 VitePress 示例内容：`My Blog`、自链接按钮和三条 `Lorem ipsum`。
- **P3-08 — `docs/.vitepress/config.mts:164,192-204`**：多项式侧栏有一个空文本/空链接项；DP 侧栏除背包外的 12 个规划条目全部跳回首页，读者无法区分“未写”和“链接故障”。建议显示纯文本“规划中”或统一 draft 标记。

`docs/markdown-examples.md` 只有一条公式，可视为渲染样例，不单独判错。

## 七、复核通过项与应撤销的旧结论

### 7.1 本轮未发现 P0/P1 的页面

以下结论表示：在正文已写出的契约和本报告的竞赛口径内，本轮静态审查没有找到需要列为 P0/P1 的当前问题；不等价于已经对拍所有功能。

- 数据结构：`01-trie.md`、`heap.md`、`cdq.md`、`lct.md`、`parallel-binary-search.md`、`segment-tree.md`、`tree-divide.md`；
- 图论：`euler.md`、`mst.md`；
- 字符串：`border.md`、`hash.md`、`manacher.md`、`string.md`；
- 数学：`dirichlet.md`、`function.md`、`prime.md`、`transfer.md`；
- 计算几何独立专题：`3d.md`、`base.md`、`circle.md`、`half.md`、`jijiao.md`、`minkowski.md`、`other.md`、`rotate.md`、`scan.md`、`tubao.md`；
- 杂项：`distance.md`、`functions.md`、`hashing.md`、`interval.md`、`tree-hash.md`；
- 多项式：`poly1.md`。

`block.md`、`tree-block.md`、`lca.md`、`Generating-function.md` 按占位页处理，不因内容少判错。

### 7.2 已确认修复或旧报告应撤销的代表项

- `cdq.md` 删除贡献循环体已经补齐；当前只剩注释边界差一位。
- `tree-divide.md` 已把“端点为重心”的路径放入 `dfs2/重心特判` 流程，旧报告不能继续要求无条件把重心塞进同一个数据结构。
- `tree-chain.md` 已处理 `k=0`，但固定 19 层和越深查询协议仍未闭环。
- `xor-base.md` 的 `query_max/query_min` 已改为 `u64`，但 `query_kth` 仍保留 32 位变量。
- `kmp.md` 已在每次 `exkmp` 调用中清零 `Z[1]`，旧值污染问题已修复。
- `manacher.md` 的镜像半径取 `min`、哨兵契约和 `len0` 注释均已修复；长度 0–8 的二进制串穷举与暴力一致。
- `acam.md` 的 BFS 逐 fail 构建可以按每条模式路径的深度势能分析做到 `O(sum |T|)`，旧报告不应再把这一本身列为复杂度错误；当前问题是扫描后的模式计数汇总。
- `euler.md` 的 Hierholzer 结果反转与边数校验已经存在。
- `mst.md` 的 Prim 访问顶点数与断图处理已修复。
- `shortest-path.md` 的普通 Bellman-Ford 备份、普通 Floyd 可达判断和差分约束方向已经修复；当前 P0 位于单独的“最小环”片段。
- `poly1.md` 中旧报告所列 32 位模乘、低次多项式除法、幂的最低项平移和拉格朗日符号/采样点问题，本轮未再复现。
- `bag.md` 的多重背包数量上限、分组下标、前后缀背包读取来源和不可单调倍增问题均已修复。

## 八、建议修复顺序

### 第一批：先清 8 个 P0

优先顺序建议为：

1. `xor-base.md`、`mod1.md`、`mod.md`、`constant.md` 的整数位宽；
2. `shortest-path.md`、`sieve.md` 的直接 UB；
3. `geometry/model.md` 的排序比较器与单点凸包。

这些问题都有很小的确定性反例，修改后适合立刻做最小样例和随机对拍。

### 第二批：统一赛时模板契约

集中处理多测/初始化、字符域、下标和存图方向：

- `SA/SAM/Trie/KMP/ACAM`；
- `merge_dsu/FastBitset/wavelet/KDTree`；
- 树上背包和长链祖先查询。

竞赛笔记不需要工业级防御式接口，但应在代码块上方用一两行明确：是否 0/1-based、是否只支持单测、是否依赖全局零初始化、合法值域/字符集、无解返回值和数组容量。

### 第三批：把“全家桶”与“参考片段”分开

`geometry/model.md` 当前同时混合整数/浮点谓词、完整结构、占位 lambda 和依赖题意的扫描线。建议至少给每个代码块标记：

- 可直接复制的模板；
- 需要补题意函数的框架；
- 只展示公式的伪代码；
- 仅适用于整数坐标或仅适用于浮点坐标。

### 第四批：用竞赛方式验证，而不是工程化过度建设

不需要为博客引入重型测试框架；更实用的是：

- 每个“完整模板”配一个能单独编译的最小 `main`；
- 对有朴素做法的模板做小规模随机对拍；
- 对整数边界保留 `INT_MIN/INT_MAX`、空集、单点、重复点、断图和多测复用样例；
- 对复杂度依赖随机性/均摊性的结构，紧邻代码写清数据模型。

## 九、最终判定

- **站点重构：通过。** 构建、路由、静态资源和旧 URL 重定向均正常。
- **文档结构：基本通过。** 少量首页、占位导航和根目录旧路径属于 P3。
- **算法正文：部分通过。** 大多数旧高危问题已经修复，但仍有 8 个 P0 和 22 个 P1。
- **赛时模板库：暂不通过。** 在 P0 清零、关键初始化/范围契约补齐，并完成至少一次实际 C++ 编译与核心模板对拍前，不宜整体宣称“全部模板已修复”。

本报告只更新 `report.md`，没有修改任何算法正文，也没有重新生成 `report.pdf`。
